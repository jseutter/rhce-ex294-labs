- name: Create crontab
  hosts: webservers

  tasks:
    - name: add cron
      ansible.builtin.cron:
        name: jseutter
        minute: "*/2"
        hour: "9-16"
        weekday: "1-5"
        user: devops
        job: df >> /home/devops/disk_usage
        cron_file: disk_usage
